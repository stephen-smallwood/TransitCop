# A script to allow configurable find and delete of directories.

Run from crontab:

50 08 * * * /usr/local/TransitCop/TransitCop.pl -v -c /usr/local/TransitCop/TransitCop.cfg > /var/log/TransitCop.log 2>&1



